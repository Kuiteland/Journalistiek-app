<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<table class="users">
  <% @users.each do |user| %>
    <tr>
      <td><%= link_to user.name, user %> </td>
      <td><%= user.email %>
    </tr>
  <% end %>
</table>

<%= will_paginate %>